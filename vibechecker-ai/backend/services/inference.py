def run_inference(image_file):
    # Placeholder — replace with your ML model
    # Example:
    # image = preprocess(image_file)
    # prediction = model.predict(image)

    return "happy"  # dummy output